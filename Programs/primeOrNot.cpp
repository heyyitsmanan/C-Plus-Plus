#include <iostream>
using namespace std;

int main()
{
    int n;
    cout << endl;
    cout << "*=*=*=*=*=*  PRIME OR NOT   *=*=*=*=*=*" << endl;
    cout << endl;
    cout << "Enter the number to be checked if it's a prime or not ? : " << endl;
    cin >> n;
    cout << endl;

    for (int i = 2; i < n; i++)
    {
        if (n % i == 0)
        {
        }
    }
}